class Obstacle {
  constructor(health, { x, y, height, width }) {
    this.health = health;
    this.x = x;
    this.y = y;
    this.height = height;
    this.width = width;
  }
  // set size/coordinates
  // check if bullet is inside of that.
}
